{
  "name": "ban-ve-su-kien",
  "version": "1.0.0",
  "description": "Hệ thống bán vé sự kiện có sơ đồ ghế",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "node --check app.js && node --check index.js && node --check db.js && node --check knexfile.js",
    "lint": "eslint .",
    "test": "node --test --test-isolation=none",
    "migrate:latest": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "knex": "^3.3.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5"
  }
}
